_: {
  vim.autocomplete.blink-cmp = {
    enable = true;

    mappings = {
      next = "<Down>";
      previous = "<Up>";
      confirm = "<Tab>";
    };

    friendly-snippets = {
      enable = true;
    };
  };
}
